#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."
mkdir -p reports/traffic

export RETRIEVAL_VERSION="${RETRIEVAL_VERSION:-v4}"
export RAG_CONTEXT_PACKING="${RAG_CONTEXT_PACKING:-1}"
export RAG_CONTEXT_MAX_CHUNKS="${RAG_CONTEXT_MAX_CHUNKS:-4}"
export RAG_EVIDENCE_COMPRESSION="${RAG_EVIDENCE_COMPRESSION:-1}"
export RAG_EVIDENCE_MAX_SPANS="${RAG_EVIDENCE_MAX_SPANS:-8}"
export RAG_EVIDENCE_MAX_CHARS="${RAG_EVIDENCE_MAX_CHARS:-900}"
export RAG_CLAUSE_AWARE_PACKING="${RAG_CLAUSE_AWARE_PACKING:-1}"
export RAG_HYBRID_FALLBACK="${RAG_HYBRID_FALLBACK:-1}"
export RAG_SPAN_RERANKING="${RAG_SPAN_RERANKING:-0}"
export RAG_CONTEXT_ORDER="${RAG_CONTEXT_ORDER:-litm}"
export RAG_LEGAL_UNIT_RETRIEVAL="${RAG_LEGAL_UNIT_RETRIEVAL:-1}"
export RAG_LEGAL_UNIT_TOP_K="${RAG_LEGAL_UNIT_TOP_K:-3}"
export RAG_LEGAL_UNIT_MAX_CANDIDATES="${RAG_LEGAL_UNIT_MAX_CANDIDATES:-80}"
export RAG_LEGAL_UNIT_APPEND_BACKUP="${RAG_LEGAL_UNIT_APPEND_BACKUP:-0}"
export RAG_LEGAL_UNIT_USE_CE="${RAG_LEGAL_UNIT_USE_CE:-0}"
export RAG_LEGAL_UNIT_CE_MODEL="${RAG_LEGAL_UNIT_CE_MODEL:-models/bge-reranker-v2-m3-traffic-ft}"
export RAG_LEGAL_UNIT_BM25_CANDIDATES="${RAG_LEGAL_UNIT_BM25_CANDIDATES:-30}"
export RAG_LEGAL_UNIT_ALLOWED_TYPES="${RAG_LEGAL_UNIT_ALLOWED_TYPES:-point,clause}"
export RAG_LEGAL_UNIT_SEED_K="${RAG_LEGAL_UNIT_SEED_K:-8}"
export RAG_EVIDENCE_CARD_RENDERING="${RAG_EVIDENCE_CARD_RENDERING:-0}"
export MAX_NEW_TOKENS="${MAX_NEW_TOKENS:-320}"
export GENERATION_REPETITION_PENALTY="${GENERATION_REPETITION_PENALTY:-1.08}"
export GENERATION_NO_REPEAT_NGRAM="${GENERATION_NO_REPEAT_NGRAM:-8}"
export RAG_V4_RRF_K="${RAG_V4_RRF_K:-60}"
export RAG_V4_W_DENSE="${RAG_V4_W_DENSE:-0.6}"
export RAG_V4_W_SPARSE="${RAG_V4_W_SPARSE:-0.4}"

python src/evaluate.py \
  --configs "${@:-A B C D}" \
  --test-file data/eval_manual.jsonl \
  --output reports/traffic/evaluation_results_eval_manual_full_optimized.json
